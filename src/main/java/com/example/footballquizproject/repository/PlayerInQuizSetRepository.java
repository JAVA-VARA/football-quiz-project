package com.example.footballquizproject.repository;

import com.example.footballquizproject.domain.PlayerInQuizSet;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PlayerInQuizSetRepository extends JpaRepository<PlayerInQuizSet, Long> {
}
